# Arthabodh UI

Frontend UI for the Arthabodh project

---

## Package Manager Installation

This project uses **pnpm** as the package manager.

### Install pnpm (if not already installed)

```bash
npm install -g pnpm

```
## Packages Installation

This project uses **pnpm** as the package manager.
```
pnpm i
